name: Decode
on:
  workflow_dispatch:

jobs:
  Decode:
    runs-on: ubuntu-latest
    steps:
    - name: Decode
      env:
        TEST_ENV: ${{ secrets.DECODE_TEST }}
      run: |
        echo $TEST_ENV | base64 -di > $SERVICE_ACCOUNT_JSON

