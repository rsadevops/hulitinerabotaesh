name: MAIN

on:
  push:
    branches: main
    
env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}
  VERSION: main
  PORT: '80:80'
  
jobs:
  call-reuse-workflow:
    permissions:
      contents: read
      packages: write
      id-token: write
    uses: ./.github/workflows/ci-cd.yml
